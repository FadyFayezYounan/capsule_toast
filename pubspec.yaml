name: capsule_toast
description: >-
  A reusable, brand-neutral Flutter package for morphing capsule notifications.
version: 0.1.0

environment:
  sdk: ^3.12.2
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

topics:
  - toast
  - notification
  - animation
  - ui

flutter:
